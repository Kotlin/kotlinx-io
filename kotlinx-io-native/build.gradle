//import org.jetbrains.kotlin.KonanTest

dependencies {
    expectedBy rootProject
}

afterEvaluate {
    apply from: rootProject.file('gradle/publish.gradle')

    if (System.getProperties().keySet().any { it.startsWith("teamcity") }) {
        tasks.findByName("runTestDebug")?.args("--ktest_logger=TEAMCITY")
    }
}

apply from: rootProject.file('gradle/bintray.gradle')

//tasks.withType(KonanTest).forEach { 
//    it.runnerLogger = "TEAMCITY"
//}

